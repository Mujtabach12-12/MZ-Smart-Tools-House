# AndroidX is required by Capacitor and its Android plugins.
android.useAndroidX=true
android.enableJetifier=true

# Keep Gradle memory usage reasonable on developer machines.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Use Android Studio/Gradle defaults unless explicitly overridden.
